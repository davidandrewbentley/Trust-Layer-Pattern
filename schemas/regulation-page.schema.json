{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Regulation Page",
  "description": "Schema for a trust-first regulation explainer page with citations, routing, and provenance.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "title",
    "last_updated",
    "reviewed_by",
    "summary",
    "body_sections",
    "citations",
    "related_courses",
    "related_regulations",
    "related_glossary_terms",
    "provenance"
  ],
  "$defs": {
    "slug": {
      "type": "string",
      "description": "Kebab-case identifier used as the unique slug for this record.",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "reviewedBy": {
      "type": "object",
      "description": "Named expert reviewer responsible for the current published version.",
      "additionalProperties": false,
      "required": [
        "name",
        "title",
        "credentials",
        "organisation"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "Full name of the reviewer."
        },
        "title": {
          "type": "string",
          "description": "Professional title or role of the reviewer."
        },
        "credentials": {
          "type": "string",
          "description": "Credentials that establish why this reviewer is qualified to sign the page."
        },
        "organisation": {
          "type": "string",
          "description": "Organisation the reviewer represents when signing the page."
        }
      }
    },
    "citation": {
      "type": "object",
      "description": "Primary or supporting source used to substantiate claims on the page.",
      "additionalProperties": false,
      "required": [
        "label",
        "url",
        "source_organisation",
        "retrieved_date",
        "supporting_quote"
      ],
      "properties": {
        "label": {
          "type": "string",
          "description": "Short label used to reference the citation from body sections."
        },
        "url": {
          "type": "string",
          "description": "Working URL for the cited source.",
          "format": "uri"
        },
        "source_organisation": {
          "type": "string",
          "description": "Organisation that published the source."
        },
        "retrieved_date": {
          "type": "string",
          "description": "Date the source was retrieved for checking.",
          "format": "date"
        },
        "supporting_quote": {
          "type": "string",
          "description": "Short quotation or extract showing the relevant supporting language from the source.",
          "minLength": 5,
          "maxLength": 240
        }
      }
    },
    "courseRouting": {
      "type": "object",
      "description": "Course or offer route surfaced when a user needs next-step guidance.",
      "additionalProperties": false,
      "required": [
        "title",
        "url",
        "provider",
        "audience"
      ],
      "properties": {
        "title": {
          "type": "string",
          "description": "Display title of the routed course or learning path."
        },
        "url": {
          "type": "string",
          "description": "Working URL for the routed offer.",
          "format": "uri"
        },
        "provider": {
          "type": "string",
          "description": "Provider responsible for delivering the course or pathway."
        },
        "audience": {
          "type": "string",
          "description": "Short description of the intended learner or buyer audience."
        }
      }
    },
    "bodySection": {
      "type": "object",
      "description": "One headed section of the regulation page body.",
      "additionalProperties": false,
      "required": [
        "heading",
        "paragraphs",
        "citations"
      ],
      "properties": {
        "heading": {
          "type": "string",
          "description": "Section heading shown in the page body."
        },
        "paragraphs": {
          "type": "array",
          "description": "Ordered paragraphs for this section.",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "A single paragraph of regulation-focused explanatory text."
          }
        },
        "citations": {
          "type": "array",
          "description": "Citation labels from the top-level citations array that support this section.",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "Citation label used within this section."
          },
          "uniqueItems": true
        }
      }
    },
    "provenance": {
      "type": "object",
      "description": "Auditable record of authorship, review timing, and fact-check outcome.",
      "additionalProperties": false,
      "required": [
        "authored_by",
        "author_type",
        "reviewed_at",
        "fact_check_passed"
      ],
      "properties": {
        "authored_by": {
          "type": "string",
          "description": "Name or role that authored the draft version of the page."
        },
        "author_type": {
          "type": "string",
          "description": "Whether the initial draft was produced by a human or an agent.",
          "enum": [
            "human",
            "agent"
          ]
        },
        "reviewed_at": {
          "type": "string",
          "description": "Timestamp for the most recent completed human review.",
          "format": "date-time"
        },
        "fact_check_passed": {
          "type": "boolean",
          "description": "Whether the page passed the hard fact-checking gate."
        }
      }
    }
  },
  "properties": {
    "slug": {
      "$ref": "#/$defs/slug"
    },
    "title": {
      "type": "string",
      "description": "Page title shown to the reader.",
      "maxLength": 80
    },
    "kicker": {
      "type": "string",
      "description": "Optional regulatory shorthand or reference code shown above the title."
    },
    "last_updated": {
      "type": "string",
      "description": "Date the page content was last updated.",
      "format": "date"
    },
    "reviewed_by": {
      "$ref": "#/$defs/reviewedBy"
    },
    "summary": {
      "type": "string",
      "description": "Quick-answer summary used as the short answer pill.",
      "minLength": 200,
      "maxLength": 400
    },
    "body_sections": {
      "type": "array",
      "description": "Ordered content sections that make up the regulation explainer.",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/bodySection"
      }
    },
    "citations": {
      "type": "array",
      "description": "Citations available to support the page body.",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/citation"
      }
    },
    "related_courses": {
      "type": "array",
      "description": "Courses or learning routes connected to the page.",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/courseRouting"
      }
    },
    "related_regulations": {
      "type": "array",
      "description": "Cross-links to related regulation slugs within the platform.",
      "items": {
        "$ref": "#/$defs/slug"
      },
      "uniqueItems": true
    },
    "related_glossary_terms": {
      "type": "array",
      "description": "Cross-links to glossary term slugs within the platform.",
      "items": {
        "$ref": "#/$defs/slug"
      },
      "uniqueItems": true
    },
    "provenance": {
      "$ref": "#/$defs/provenance"
    }
  }
}
